# AGENTS.md

## Project Overview

这是微信机器人后端服务，也是机器人的管理后台，负责管理不同的机器人客户端/服务端，使用 Go、Gin、GORM、Redis，程序入口是 `main.go`。

本服务有些接口需要转发到微信客户端服务，由于历史包袱的原因，这些接口需要在 query 参数中必须携带一个 id 参数，这个参数是机器人实例的 id，表示要转发到哪个机器人客户端。因此，这个 id 很容易跟业务 id 冲突，请求的 query 参数，业务 id 参数需要加上业务前缀，需要特别注意。接口的 body 参数、接口返回的 id 不受这个影响。
