GIN_MODE=debug

# 本地调试微信机器人客户端，这里的9001端口是你本地启动微信机器人客户端服务(wechat-robot-client)的端口
DEV_ROBOT_CLIENT_URL=http://127.0.0.1:9001/api/v1/robot

#docker 网络，这里可以写死
DOCKER_NETWORK=wechat-robot

# Session，随机字符串就行
SESSION_SECRET=sessionid_f090b5ed-61ef

# AI聊天的OpenAI密钥
OPENAI_API_KEY=sk-xxxxxxxx

LOGIN_METHOD="scan" # 登录方式，scan 表示扫码登录，token 表示密码登录
LOGIN_TOKEN="your_login_token" # 登录时使用的 token
WECHAT_SERVER_ADDRESS=127.0.0.1  # 微信认证服务器地址，扫码登录管理后台用的
WECHAT_SERVER_TOKEN=sk_xxxxxx  # 微信认证密钥
WECHAT_OFFICIAL_ACCOUNT_AUTH_URL=https://img.houhoukang.com/uranus/qrcode_for_gh.jpg   # 扫码登录时的公众号二维码图片地址

# 安全验证相关
SLIDER_VERIFY_URL=http://wechat-slider:9000/api/v1/slider-verify-html # 获取滑块验证的URL (GET请求)，如果没有对接第三方滑块的诉求，这里不用修改
SLIDER_VERIFY_SUBMIT_URL=http://wechat-slider:9000/api/v1/security-verify # 提交滑块验证的URL (POST请求)，如果没有对接第三方滑块的诉求，这里不用修改

# mysql 配置
MYSQL_DRIVER=mysql
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_USER=houhou
MYSQL_PASSWORD=houhou
MYSQL_DB=robot_admin # 可以固定写死
MYSQL_SCHEMA=public # 可以固定写死

# redis 配置
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=houhou

# 非核心功能，可以先不管
THIRD_PARTY_API_KEY=0000000000000000
